<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc><%= config.url %></loc>
    <lastmod><%= date(Date.now(), 'YYYY-MM-DD') %></lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  <% if (config.root) { %>
  <url>
    <loc><%= url_for(config.root) %></loc>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
  <% } %>
  <% site.pages.forEach(function(page) { %>
  <url>
    <loc><%= url_for(page.path) %></loc>
    <lastmod><%= date(page.updated || page.date, 'YYYY-MM-DD') %></lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.6</priority>
  </url>
  <% }); %>
  <% site.posts.forEach(function(post) { %>
  <url>
    <loc><%= url_for(post.path) %></loc>
    <lastmod><%= date(post.updated || post.date, 'YYYY-MM-DD') %></lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  <% }); %>
</urlset>
